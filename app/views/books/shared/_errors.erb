<div id="errors">
  <% if errors %>
    <h2><%= pluralize(errors.count, "error") %></h2>
    <ul>
      <% errors.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  <% end %>
</div>
